import Searcher from "../ui/Searcher";

const Header = () => {
    return (
        <header>
            <div>
                <div>
                    <p>P</p>

                    <Searcher />

                    {/* Nav here */}
                </div>
                <div>{/* Administration Menu here */}</div>
            </div>
        </header>
    );
};

export default Header;
